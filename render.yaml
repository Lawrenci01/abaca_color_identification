services:
  - type: web
    name: abaca-scanner
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python inference_server.py
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0